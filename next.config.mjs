/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com" , "links.papareact.com"]  //định nghĩa đường dẫn
    }

};

export default nextConfig;
